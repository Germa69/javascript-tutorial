# DOM CSS

```
- Là một thuộc tính nằm trong element node
- Lấy ra được value và set value cho những thuộc tính đó
- Khi element.style trả lại 1 đối tượng webAPI  ( CSSStyleDeclaration ) 
- Các thuộc tính được viết theo kiểu Camel Case

- assign(): hàm gán, nhận nhiều đối số
- Chấm style là lấy ra thuộc tính CSS inline của nó

=> assign(Object, Object (CSS))
```